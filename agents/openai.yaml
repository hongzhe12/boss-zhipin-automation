interface:
  display_name: "Boss直聘自动打招呼"
  short_description: "用 ADB 驱动安卓手机在 Boss直聘自动打招呼，附带 Android 自动化开发与排查"
  default_prompt: "使用 $boss-zhipin-automation 先检查 ADB 手机连接和 Boss直聘职位详情页状态；检查通过后运行 scripts/run_auto_greet.cmd 自动打招呼 10 个职位，连接异常或当前不是职位详情页时停止并报告。"
policy:
  allow_implicit_invocation: true
